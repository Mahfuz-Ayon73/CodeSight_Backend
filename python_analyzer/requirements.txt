# Core dependencies — all have pre-built cp314 wheels, no compilation needed
tree-sitter==0.23.2
tree-sitter-javascript==0.23.1
tree-sitter-typescript==0.23.2
networkx==3.4.2

# Scientific computing — cp314 wheels available
numpy>=2.3.0
scikit-learn>=1.5.0
hdbscan>=0.8.43

# Embeddings
sentence-transformers==3.3.1

# FastAPI microservice
fastapi==0.115.6
uvicorn==0.32.1
python-dotenv==1.0.1

# NOTE: leidenalg and igraph are NOT included — no cp314 wheels exist for them.
# The system automatically falls back to NetworkX connected-components clustering.
# If you want Leiden clustering later, install Python 3.11 alongside 3.14 and
# create a separate venv: python3.11 -m venv venv311
