# Core dependencies — all have pre-built cp314 wheels, no compilation needed
tree-sitter==0.23.2
tree-sitter-javascript==0.23.1
tree-sitter-typescript==0.23.2
networkx==3.4.2

# Scientific computing — cp314 wheels available
numpy>=2.3.0
scikit-learn>=1.5.0
hdbscan>=0.8.43

# Dimensionality reduction for multi-view clustering matrix
# UMAP compresses 384-dim embeddings → 3 dense components before HDBSCAN
# Falls back to PCA (scikit-learn) if unavailable
umap-learn>=0.5.6

# Embeddings
sentence-transformers==3.3.1

# FastAPI microservice
fastapi==0.115.6
uvicorn==0.32.1
python-dotenv==1.0.1

# NOTE: leidenalg and igraph are NOT included — no cp314 wheels exist for them.
# The system automatically falls back to Louvain (NetworkX) clustering.
